using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WidthChecker : MonoBehaviour
{
    /*public bool brickFound;
    Brick detectedBrick;
    public bool shouldDisplay = true;

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.GetComponent<Brick>()) detectedBrick = collision.GetComponent<Brick>();
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        if (detectedBrick != null && collision.gameObject == detectedBrick.gameObject) {
            detectedBrick = null;
        }
    }

    private void Update()
    {
        brickFound = detectedBrick != null;
        Color invisibleColor = new Color(0, 0, 0, 0);
        GetComponent<SpriteRenderer>().color = brickFound ? (shouldDisplay ? Color.white : invisibleColor) : invisibleColor;
    }*/
}
